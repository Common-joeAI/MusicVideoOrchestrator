# Core dependencies
PyQt5>=5.15.0
numpy>=1.19.0
opencv-python>=4.5.0
tqdm>=4.50.0
websocket-client>=1.2.0
requests>=2.25.0

# Image processing
Pillow>=8.0.0

# File handling
pathlib>=1.0.1

# System utilities
psutil>=5.8.0

# Optional: for better logging

# ComfyUI dependencies are installed separately in the ComfyUI directory
# RIFE dependencies are installed separately in the RIFE directory

torch~=2.7.1
torchvision~=0.22.1
aiohttp~=3.12.13
typing_extensions~=4.14.0
torchaudio~=2.7.1
einops~=0.8.1
PyYAML~=6.0.2
safetensors~=0.5.3
transformers~=4.52.4
tokenizers~=0.21.1